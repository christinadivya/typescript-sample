"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readTsConfig = void 0;
const locter_1 = require("locter");
const node_path_1 = __importDefault(require("node:path"));
async function readTsConfig(directory) {
    directory = directory || process.cwd();
    directory = node_path_1.default.isAbsolute(directory) ? directory : node_path_1.default.join(process.cwd(), directory);
    const filePath = directory.indexOf('.json') === -1 ?
        node_path_1.default.join(directory, 'tsconfig.json') :
        directory;
    try {
        const tsConfig = await (0, locter_1.load)(filePath);
        // todo: maybe follow extends chain ;)
        if (typeof tsConfig === 'object') {
            return tsConfig;
        }
    }
    catch (e) {
        // don't do anything ;)
    }
    return {};
}
exports.readTsConfig = readTsConfig;
