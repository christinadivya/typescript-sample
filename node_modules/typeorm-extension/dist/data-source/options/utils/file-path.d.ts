import type { DataSourceOptions } from 'typeorm';
import type { SeederOptions } from '../../../seeder';
export declare function adjustFilePathsForDataSourceOptions<T extends Partial<DataSourceOptions> & SeederOptions>(input: T, options?: {
    root?: string;
    keys?: string[];
}): Promise<T>;
