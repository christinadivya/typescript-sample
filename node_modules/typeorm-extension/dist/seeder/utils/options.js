"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setDefaultSeederOptions = void 0;
const env_1 = require("../../env");
function setDefaultSeederOptions(options) {
    if (!Array.isArray(options.factories) ||
        options.factories.length === 0) {
        let factories = (0, env_1.useEnv)('factories');
        if (factories.length === 0) {
            factories = ['src/database/factories/**/*{.ts,.js}'];
        }
        Object.assign(options, {
            factories,
        });
    }
    if (!Array.isArray(options.seeds) ||
        options.seeds.length === 0) {
        let seeds = (0, env_1.useEnv)('seeds');
        if (seeds.length === 0) {
            seeds = ['src/database/seeds/**/*{.ts,.js}'];
        }
        Object.assign(options, {
            seeds,
        });
    }
    return options;
}
exports.setDefaultSeederOptions = setDefaultSeederOptions;
